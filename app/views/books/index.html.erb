<div class="m-4">
 <p class="text-success" id="notice"><%= notice %></p>
</div>
<div class="container-fluid">
  <div class="mt-4 ml-2">
    <h2><strong>Books</strong></h2>
  </div>
</div>

<div class="container-fluid">
  <div class="ml-3">
   <div class="row justify-content-around">
      <table>
        <thead>
          <tr>
            <div class="col-sm-2">
              <div class="text-center"><strong>Title</strong></div>
            </div>
            <div class="col-sm-3">
              <div class="text-center"><strong>Body</strong></div>
            </div>
            <div class="col-sm-1" colspan="3"></div>
            <div class="col-sm-6"></div>
          </tr>
        </thead>
      </table>
    </div>
  </div>
</div>

<tbody>
  <div class="container-fluid">
    <div class="ml-3">
      <div class="row justify-content-around">
        <% @books.each do |book| %>
          <tr>
            <p>
              <div class="col-sm-2"><%= book.title %></div>
              <div class="col-sm-3"><%= book.body %></div>
              <div class="col-sm-1">
                <%= link_to "show", book_path(book.id) %>
                <%= link_to "edit", edit_book_path(book) %>
                <%= link_to "destroy", book, method: :delete, date: {　confirm: 'Are you sure?' } %>
              </div>
              <div class="col-sm-6"></div>
            </p>
          </tr>
        <% end %>
      </div>
    </div>
  </div>
</tbody>
<p>
</p>
<div class="container-fluid">
  <div class="ml-3">
   <h5><strong>New book</storng></h5>
    <%= form_with model:@book, local:true do |f| %>
      <% if @book.errors.any? %>
        <%= @book.errors.count %>errors prohibited this obj from being saved:<br>
          <% @book.errors.full_messages.each do |message| %>
            <%= message %><br>
          <% end %>
      <% end %>
      <div class="mt-3">
        <h6>Title</h6>
        <%= f.text_field :title %>
      
        <h6>Body</h6>
        <%= f.text_area :body %>
      <br>
        <%= f.submit 'Create Book' %>
      </div>
    <% end %>
 </div>
</div>